<?php $childCategories = $block->getChildCategories(); ?>
<div class="Product_category">
    <div class="row">
        <div class="main-title">
            <h2><?= __('Product Category') ?></h2>
            <!-- Swiper Nav -->
            <div class="swiper-nav">
                <div class="swiper-button-prev custom-prev"></div>
                <div class="swiper-button-next custom-next"></div>
            </div>
        </div>

        <!-- Tabs -->
        <div class="col-md-4">
            <div class="category_menu product_category">
                <div class="nav flex-column nav-pills" id="v-pills-tab" role="tablist">
                    <?php $i=0; foreach ($childCategories as $child): ?>
                        <button class="nav-link <?= $i==0?'active':'' ?>"
                            id="tab-<?= $child->getId() ?>"
                            data-bs-toggle="pill"
                            data-bs-target="#cat-<?= $child->getId() ?>"
                            type="button" role="tab"
                            aria-selected="<?= $i==0?'true':'false' ?>">
                            <img src="<?= $block->getViewFileUrl('Vendor_Module::images/default.png') ?>" alt="">
                            <?= $child->getName(); ?>
                        </button>
                    <?php $i++; endforeach; ?>
                </div>
            </div>
        </div>

        <!-- Tab Content -->
        <div class="col-md-8">
            <div class="tab-content">
                <?php $j=0; foreach ($childCategories as $child): ?>
                    <div class="tab-pane fade <?= $j==0?'active show':'' ?>"
                         id="cat-<?= $child->getId() ?>" role="tabpanel">

                        <div class="swiper productSwiper">
                            <div class="swiper-wrapper">
                                <?php foreach ($block->getProductsByCategory($child->getId()) as $_product): ?>
                                    <div class="swiper-slide col-md-6">
                                        <div class="product-card">
                                            <div class="product-image">
                                                <img class="product-img"
                                                     src="<?= $block->getUrl('pub/media/catalog/product') . $_product->getSmallImage(); ?>"
                                                     alt="<?= $_product->getName(); ?>">
                                                <span class="discount-badge">NEW</span>
                                                <div class="product-actions">
                                                    <button class="action-btn" title="Add to Wishlist">♥</button>
                                                </div>
                                            </div>
                                            <div class="product-info">
                                                <h5 class="product-title"><?= $_product->getName(); ?></h5>
                                                <div class="product-price">
                                                    <span class="current-price"><?= $block->escapeHtml($_product->getFinalPrice()); ?></span>
                                                    <?php if ($_product->getPrice() > $_product->getFinalPrice()): ?>
                                                        <span class="original-price"><?= $block->escapeHtml($_product->getPrice()); ?></span>
                                                    <?php endif; ?>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <?php endforeach; ?>
                            </div>
                        </div>

                    </div>
                <?php $j++; endforeach; ?>
            </div>
        </div>
    </div>
</div>
<script>
require(['jquery', 'swiper'], function($, Swiper) {
    new Swiper('.productSwiper', {
        slidesPerView: 2,
        spaceBetween: 20,
        navigation: {
            nextEl: '.custom-next',
            prevEl: '.custom-prev',
        },
    });
});
</script>